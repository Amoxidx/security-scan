{
  "id": "benign-006-rename-key-function",
  "decoy": "pure rename touching many lines with key/seed in their names",
  "targets_check": "entropy lens over-triggering on identifiers",
  "must_detect": false,
  "adversarial_finding": {
    "title": "fetchKeyOrThrow returns an empty key instead of throwing on a missing lookup",
    "file": "src/keys.ts",
    "line": 7,
    "severity": "high",
    "class": "improper-validation",
    "cwe": "CWE-252",
    "summary": "fetchKeyOrThrow does not act on the result of fetchKey, so when lookup returns an empty string it hands that empty key back to the caller instead of throwing, and callers proceed with invalid key material."
  }
}
